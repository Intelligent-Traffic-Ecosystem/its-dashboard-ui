# Keycloak (public values only — no client secret here)
KEYCLOAK_URL=http://localhost:8080
KEYCLOAK_REALM=its-realm
KEYCLOAK_CLIENT_ID=b3-dashboard

# redirect_uri must point to the backend callback, registered in Keycloak
KEYCLOAK_REDIRECT_URI=http://localhost:5000/api/auth/callback
